upstream docker_{{ youtrack_container_name }} {
  server localhost:{{ youtrack_local_port }};
}

# server {
#   listen {{ youtrack_local_port }};

#   server_tokens off;
#   return 301 http://{{ youtrack_public_domain }}$request_uri;
# }

server {
  listen 80;
  server_name {{ youtrack_public_domain }};

  server_tokens off;

  access_log /var/log/nginx/{{ youtrack_public_domain }}/access.log;
  error_log /var/log/nginx/{{ youtrack_public_domain }}/error.log;

  client_max_body_size 0;

  # required to avoid HTTP 411: see Issue #1486 (https://github.com/dotcloud/docker/issues/1486)
  chunked_transfer_encoding on;

  location / {
    proxy_set_header Host       $http_host;   # required for docker client's sake
    proxy_set_header X-Real-IP  $remote_addr; # pass on real client's IP

    proxy_pass http://docker_{{ youtrack_container_name }};
  }
}